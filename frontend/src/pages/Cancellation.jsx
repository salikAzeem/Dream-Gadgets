import PolicyLayout from "../components/PolicyLayout";

export default function Cancellation() {
  return (
    <PolicyLayout title="Cancellation Policy">
      <p>
        Orders can be cancelled before shipping. Once shipped, cancellation is
        not allowed.
      </p>
    </PolicyLayout>
  );
}
